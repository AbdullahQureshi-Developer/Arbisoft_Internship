# prompt:1

tell me about Transformer/attention intuition: what makesLLMs work & Tokenization, context windows,temperature, sampling

## prompt:2

And what are Embeddings: how they are used &
Prompt fundamentals: system prompts,few-shot, chain-of-thought

## prompt:3

Build a Python CLI chatbot using OpenRouter API with httpx that streams responses chunk by chunk using SSE, stores multi-turn conversation history, and handles HTTP errors gracefully.

## prompt:4

Create an OpenRouterClient dataclass in Python that loads API key and model from environment variables, builds the messages payload, and supports both streaming and non-streaming responses.

## prompt:5

Add slash commands to my CLI chatbot — /help, /clear, /model, /quit — using Rich library for colored output and panels, with a welcome screen showing the current model.

## prompt:6

Test my CLI chatbot against llama3, mistral, and phi3 models on OpenRouter by asking about embeddings and RAG, then save each model's response and runtime to a separate txt file.
